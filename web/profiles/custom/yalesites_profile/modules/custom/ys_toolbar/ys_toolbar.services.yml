services:
  # A service for adding custom editorial tasks to the admin toolbar.
  ys_toolbar.items:
    class: Drupal\ys_toolbar\ToolbarItemsService
    arguments: ['@access_manager', '@current_route_match', '@current_user']
