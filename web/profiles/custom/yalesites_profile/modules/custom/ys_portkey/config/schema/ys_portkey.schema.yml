ys_portkey.settings:
  type: config_object
  label: 'Portkey Settings'
  mapping:
    data:
      type: string
      label: 'Placeholder config. Per-model settings are stored in ai.settings.'
    api_key:
      type: string
      label: 'Legacy API key (pre-migration)'
    gateway_url:
      type: string
      label: 'Legacy gateway URL (pre-migration)'
    model:
      type: string
      label: 'Legacy model ID (pre-migration)'
    custom_headers:
      type: string
      label: 'Legacy custom headers (pre-migration)'
