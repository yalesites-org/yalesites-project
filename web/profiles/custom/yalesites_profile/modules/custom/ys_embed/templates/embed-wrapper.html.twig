{% if display_attributes['isIframe'] %}
  <iframe src="{{ url }}" title="{{ title }}" height="{{ display_attributes['height'] }}" width="{{ display_attributes['width'] }} scrolling="{{ display_attributes['scrolling'] }}" frameborder="{{ display_attributes['frameborder'] }}"></iframe>   
{% else %}
  {{ embedSource }}
{% endif %}
